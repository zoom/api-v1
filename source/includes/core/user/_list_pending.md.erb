## List Pending Users

List all the pending users on Zoom

> Definition

```shell
POST <%= config[:api_host] %>user/pending
```

```javascript
Zoom.user.listPending();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>user/pending \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %>
```

```javascript
<%= config[:ex_node_init] %>

Zoom.user.listPending(<%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 5,
  "total_records": 235,
  "page_number": 1,
  "page_size": 50,
  "users": [
    {
        "id": "<%= config[:ex_uuid] %>",
        "email": "<%= config[:ex_email] %>",
        "first_name": "Zoom",
        "last_name": "User",
        "type": 1,
        "enable_large": false,
        "large_capacity": 0, 
        "enable_webinar": false, 
        "webinar_capacity": 0, 
        "verified": 1,
        "created_at": "2012-11-25T12:00:00Z",
        "status" : 0
    }
  ]
}
```

Arguments | &nbsp; 
-------- | -----------
page_size *optional*    | The amount of records returns within a single API call. Max of 300 users. *Default: 30*
page_number *optional*  | Current page number of returned records. Default to 1. *Default: 1*
