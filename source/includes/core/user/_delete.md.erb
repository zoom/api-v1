## Delete a User

Delete a user on Zoom, return the deleted user ID.

> Definition

```shell
POST <%= config[:api_host] %>user/delete
```

```javascript
Zoom.user.delete();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>user/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

Zoom.user.delete({id: '<%= config[:ex_uuid] %>'}, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "account_id": " <%= config[:ex_uuid] %>",
  "deleted_at": "2012-11-25T12:00:00Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | User id.
type *optional* | 0 means existing user, 1 means pending user. *Default: 0*