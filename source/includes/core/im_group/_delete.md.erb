## Delete IM Group

Delete a IM group on Zoom, return the deleted group ID.

> Definition

```shell
POST <%= config[:api_host] %>im/group/delete
```

```javascript
Zoom.imGroup.delete();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>im/group/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

var group = {
    id: "<%= config[:ex_uuid] %>"
}

Zoom.imGroup.delete(group, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "deleted_at": "2015-08-20T08:41:58Z",
  "deleted_count": 1
}
```

Arguments | &nbsp;
--- | ---
id **required** | Group ID.