## Get Ended Webinar UUID

Get ended webinar UUID.

> Definition

```shell
POST <%= config[:api_host] %>webinar/uuid/list
```

```javascript
Zoom.webinar.getUUID();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/uuid/list \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>"
}

Zoom.webinar.getUUID(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "webinars": [
    {
      "uuid": "<%= config[:ex_uuid] %>",
      "start_time": "2015-08-23T03:03:48Z"
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.