## Get Webinar Info

Get a Webinar on Zoom via Webinar ID, return the Webinar info

> Definition

```shell
POST <%= config[:api_host] %>webinar/get
```

```javascript
Zoom.webinar.get();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/get \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>"
}

Zoom.webinar.get(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "uuid": "<%= config[:ex_uuid] %>",
  "id": "<%= config[:ex_meeting_number] %>",
  "start_url": "https://zoom.us/s/<%= config[:ex_meeting_number] %>?zpk=hs65q23kd9sqliy612h23k",
  "join_url": "https://zoom.us/j/<%= config[:ex_meeting_number] %>",
  "registration_url": "",
  "created_at": "2012-11-25T12:00:00Z",
  "host_id": "<%= config[:ex_uuid] %>",
  "topic": "Topic for this meeting",
  "type": 5,
  "start_time": "2012-11-25T12:00:00Z",
  "duration": 30,
  "timezone": "America/Los_Angeles",
  "agenda": "",
  "option_start_type": "video",
  "option_audio": "both",
  "option_enforce_login": false,
  "option_enforce_login_domains ": "",
  "option_alternative_hosts": "",
  "status": 0,
  "occurrences": [
    {
      "occurrence_id": "1483210260000",
      "start_time": "2017-01-01T02:51:00Z",
      "duration": 60
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.