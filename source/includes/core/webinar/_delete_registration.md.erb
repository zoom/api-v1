## Cancel Webinar Registration

Cancel Webinar Registration

> Definition

```shell
POST <%= config[:api_host] %>webinar/registration/cancel
```

```javascript
Zoom.webinar.cancelRegistration();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/registration/cancel \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
    -d registrants_ids="<%= config[:ex_uuid] %>,<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>",
    registrants_ids: "<%= config[:ex_uuid] %>,<%= config[:ex_uuid] %>"
}

Zoom.webinar.cancelRegistration(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "844495467",
  "updated_at": "2016-11-29T01:26:43Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.
occurrence_id *optional* | Occurrence ID, could get this value from Webinar Get API.
registrant_ids **required** | Webinar registrant IDs, separated by comma. Max of 15 registrants.