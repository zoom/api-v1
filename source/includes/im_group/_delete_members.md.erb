## Delete IM Group Members

Delete members of a IM group on Zoom.

> Definition

```shell
POST <%= config[:api_host] %>im/group/member/delete
```

```javascript
Zoom.imGroup.memberDelete();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>im/group/member/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d member_ids="<%= config[:ex_uuid] %>,<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var group = {
    id: "<%= config[:ex_uuid] %>",
    member_ids: "<%= config[:ex_uuid] %>,<%= config[:ex_uuid] %>"
}

Zoom.imGroup.memberDelete(group, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "deleted_at": "2015-08-20T08:57:38Z",
  "deleted_count": 2
}
```

Arguments | &nbsp;
--- | ---
id **required** | Group ID.
member_ids **required** | The member IDs, separated by comma