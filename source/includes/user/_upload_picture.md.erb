## Upload User Picture

Upload and update user's profile picture on Zoom via user ID.

<aside class="notice">This should be multipart form data and "pic_file" is the actual contents of the image file</aside>

> Definition

```shell
POST <%= config[:api_host] %>user/uploadpicture
```

> Example Request

```shell
$ curl <%= config[:api_host] %>user/uploadpicture \
    -F api_key=<%= config[:ex_api_key] %> \
    -F api_secret=<%= config[:ex_api_secret] %> \
    -F id=<%= config[:ex_uuid] %> \
    -F pic_file="@/path/to/pic.jpg"
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "updated_at": "2013-09-02T12:00:00Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | User id.
pic_file **required** | User picture file, must be a jpg/jpeg file.