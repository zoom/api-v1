## Delete User Assistant

Delete a user's assistant.

> Definition

```shell
POST <%= config[:api_host] %>user/assistant/delete
```

```javascript
Zoom.user.deleteAssistant();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>user/assistant/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

var user = {
    host_email: "<%= config[:ex_email] %>"
}

Zoom.user.deleteAssistant(user, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "host_email": "<%= config[:ex_email] %>",
  "updated_at": "2013-09-02T12:00:00Z"
}
```

<aside class="notice">Either <strong>id</strong> or <strong>host_email</strong> is required, if id is passed then host_email is ignored</aside>

Arguments | &nbsp;
--- | ---
id *optional* | The host’s user id.
host_email *optional* | The host’s email address.