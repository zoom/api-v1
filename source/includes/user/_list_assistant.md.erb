## List Assigned Schedulers

List assigned schedule privilege for host users.

> Definition

```text
POST <%= config[:api_host] %>user/scheduleforhost/list
```

> Example Request

```shell
$ curl <%= config[:api_host] %>user/scheduleforhost/list \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

> Example Response

```json
{
  "total_records": 1,
  "users": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "email": "<%= config[:ex_email] %>",
      "first_name": "<%= config[:ex_first_name] %>",
      "last_name": "<%= config[:ex_last_name] %>"
    }
  ]
}
```

<aside class="notice">Either <strong>id</strong> or <strong>host_email</strong> is required, if id is passed then host_email is ignored</aside>

Arguments | &nbsp;
--- | ---
id *optional* | The host’s user id.
host_email *optional* | The host’s email address.