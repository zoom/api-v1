## Get CRC Port Usage

Get CRC Port usage hour by hour for a specified time period.

> Definition

```shell
POST <%= config[:api_host] %>metrics/crc
```

```javascript
Zoom.dashboard.crc();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>metrics/crc \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d from="2015-03-12" \
    -d to="2015-03-12"
```

```javascript
<%= config[:ex_node_init] %>

var dashboard = {
    from: "2013-01-11T08:18:09Z",
    to: "2013-02-11T08:18:09Z"
}

Zoom.dashboard.crc(dashboard, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "from": "2015-03-12",
  "to": "2015-03-12",
  "crc_ports_usage": [
    {
      "date_time": "2015-03-12",
      "crc_ports_hour_usage": [
        {
          "hour": "00",
          "max_usage": 1,
          "total_usage": 3
        }
      ]
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
from **required** | Start date.
to **required** | End date.