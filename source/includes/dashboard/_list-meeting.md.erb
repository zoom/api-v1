## Get Meeting List

Get Live Meetings and past meetings for a specified period

> Definition

```shell
POST <%= config[:api_host] %>metrics/meetings
```

```javascript
Zoom.dashboard.meetings();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>metrics/meetings \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d type=1 \
    -d from="2015-03-12" \
    -d to="2015-03-12"
```

```javascript
<%= config[:ex_node_init] %>

var dashboard = {
    type: 1,
    from: "2013-01-11T08:18:09Z",
    to: "2013-02-11T08:18:09Z"
}

Zoom.dashboard.meetings(dashboard, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "from": "2015-08-02",
  "to": "2015-09-02",
  "page_count": 2,
  "page_number": 1,
  "page_size": 30,
  "total_records": 49,
  "type": "Past Meetings",
  "meetings": [
    {
      "uuid": "<%= config[:ex_uuid] %>",
      "id": "<%= config[:ex_meeting_number] %>",
      "host": "<%= config[:ex_first_name] %> <%= config[:ex_last_name] %>",
      "email": "<%= config[:ex_email] %>",
      "user_type": "Pro",
      "start_time": "2015-08-29T05:36:56Z",
      "end_time": "2015-08-29T06:36:56Z",
      "duration": "4 mins",
      "participants": 2,
      "has_pstn": false,
      "has_voip": true,
      "has_3rd_party_audio": false,
      "has_video": true,
      "has_screen_share": false,
      "recording": 0
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
type **required** | Meeting list type. 1 means live meetings. 2 means past meetings. *Default: 1*
from **required** | Start date. Only for past meetings.
to **required** | End date. Only for past meetings.
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. Max of 100 meetings. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*