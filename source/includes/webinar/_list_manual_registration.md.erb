## List Webinar Registrants

List webinar registrants on Zoom, include pending approval registrants and approved registrants.

> Definition

```shell
POST <%= config[:api_host] %>webinar/registrants/list
```

```javascript
Zoom.webinar.listRegistrations();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/registrants/list \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>"
}

Zoom.webinar.listRegistrations(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 1,
  "type": 0,
  "users": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "first_name": "<%= config[:ex_first_name] %>",
      "last_name": "<%= config[:ex_last_name] %>",
      "email": "<%= config[:ex_email] %>",
      "registration_time": "2016-08-18T01:45:26Z"
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.
type *optional* | 0 means pending approval registrants, 1 means approved registrants. *Default: 0*
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*