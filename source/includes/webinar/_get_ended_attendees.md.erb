## Get Ended Webinar Attendees

Get ended webinar attendees

> Definition

```text
POST <%= config[:api_host] %>webinar/attendees/list
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/attendees/list \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

> Example Response

```json
{
  "attendees": [
    {
      "first_name": "<%= config[:ex_first_name] %>",
      "last_name": "<%= config[:ex_last_name] %>",
      "email": "<%= config[:ex_email] %>",
      "join_time": "2015-09-18T09:41:39Z",
      "leave_time": "2015-09-18T09:42:12Z",
      "time_in_session": "8",
      "attended": "Yes",
      "custom_questions": {
        "ssssss": " sssss",
        "which one do you like?": "sss"
      },
      "questions": {
        "country": "US",
        "address": "1234 Some Street",
        "city": "Zoomville"
      }
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.
uuid *optional* | Webinar uuid. It can be fetched by /v1/webinar/uuid/list.