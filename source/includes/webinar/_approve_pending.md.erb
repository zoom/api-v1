## Approve Pending Approval Registrants

Approve pending approval registrants on Zoom Webinar.

> Definition

```text
POST <%= config[:api_host] %>webinar/registrants/approve
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/registrants/approve \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>" \
    -d registrants_ids="<%= config[:ex_uuid] %>,<%= config[:ex_uuid] %>"
```

> Example Response

```json
{
  "id": "421431292",
  "updated_at": "2016-08-18T02:11:40Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.
registrant_ids **required** | Pending approval registrant ids.