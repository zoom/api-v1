## Get Ended Webinar Q&A

Get Ended Webinar Q&A

> Definition

```shell
POST <%= config[:api_host] %>webinar/questions
```

```javascript
Zoom.webinar.getQuestions();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/questions \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>"
}

Zoom.webinar.getQuestions(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_meeting_number] %>",
  "uuid": "<%= config[:ex_uuid] %>",
  "start_time": "2017-02-09T07:15:48Z",
  "questions": [
    {
      "name": "<%= config[:ex_first_name] %> <%= config[:ex_last_name] %>",
      "email": "<%= config[:ex_email] %>",
      "question_details": [
        {
          "question": "question?",
          "answer": "answer"
        }
      ]
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.
uuid *optional* | Webinar uuid. It can be fetched by /v1/webinar/uuid/list.