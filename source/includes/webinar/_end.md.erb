## End A Webinar

End a Webinar on Zoom, return the deleted Webinar ID

> Definition

```shell
POST <%= config[:api_host] %>webinar/end
```

```javascript
Zoom.webinar.end();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>webinar/end \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id="<%= config[:ex_uuid] %>" \
    -d host_id="<%= config[:ex_uuid] %>"
```

```javascript
<%= config[:ex_node_init] %>

var webinar = {
    id: "<%= config[:ex_uuid] %>",
    host_id: "<%= config[:ex_uuid] %>"
}

Zoom.webinar.end(webinar, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_meting_number] %>",
  "ended_at": "2012-11-25T12:00:00Z"
}
```

Arguments | &nbsp;
--- | ---
id **required** | Webinar ID.
host_id **required** | Webinar host user ID. Can be any user who enabled webinar under this account.