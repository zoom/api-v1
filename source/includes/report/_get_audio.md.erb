## Get Audio Report

Get user report for a specified period.

<aside class="notice">Toll Report API is provided for enabled 'Toll Report' option.</aside>

<aside class="notice">We will report a maximum of one month. For example, if "from" is set to "2015-08-05" and "to" is "2015-10-10" we will adjust "from" to "2015-09-10"</aside>

> Definition

```shell
POST <%= config[:api_host] %>report/getaudioreport
```

```javascript
Zoom.report.getAudio();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>report/getaudioreport \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d from="2013-01-11T08:18:09Z" \
    -d to="2013-02-11T08:18:09Z"
```

```javascript
<%= config[:ex_node_init] %>

var report = {
    from: "2013-01-11T08:18:09Z",
    to: "2013-02-11T08:18:09Z"
}

Zoom.report.getAudio(report, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "from": "2016-01-08",
  "to": "2016-01-21",
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 1,
  "telephony_usage": [
    {
      "meeting_id": 2932547621,
      "phone_number": "+86 8208",
      "type": "call-out",
      "host_name": "test",
      "host_email": "zoom.test@zoom.us",
      "department": "",
      "start_time": "2016-01-12T02:25:34Z",
      "end_time": "2016-01-12T02:26:48Z",
      "duration": 2,
      "total": 0.12
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
from **required** | Start date.
to **required** | End date.
type *optional* | Audio type: 1 means Toll-free Call-in & Call-out. 2 means Toll. *Default: 1*
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. Max of 300 meetings. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*