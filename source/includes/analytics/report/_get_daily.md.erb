## Get Daily Report

Get daily report for one month, can only get daily report for recent 3 months.

> Definition

```shell
POST <%= config[:api_host] %>report/getdailyreport
```

```javascript
Zoom.report.getDaily();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>report/getdailyreport \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d year=2013 \
    -d month=5
```

```javascript
<%= config[:ex_node_init] %>

var report = {
    year: 2017,
    month: 6
}

Zoom.report.getDaily(report, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "year": 2013,
  "month": 5,
  "dates": [
    {
      "meetings": 0,
      "date": "2013-05-01",
      "new_user": 0,
      "participants": 0,
      "meeting_minutes": 0
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
year **required** | Year for this report.
month **required** | Month for this report.