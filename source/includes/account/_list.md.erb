## List sub account

List all the sub account under the master account.

> Definition

```text
POST <%= config[:api_host] %>ma/account/list
```

> Example Request

```shell
$ curl <%= config[:api_host] %>ma/account/list \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %>
```

```javascript
<%= config[:ex_node_init] %>

Zoom.account.list(<%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 3,
  "subAccounts": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "owner_email": "<%= config[:ex_email] %>",
      "created_at": "2015-01-27T140508Z"
    }
  ]
}
```

Arguments | &nbsp;
--- | ---