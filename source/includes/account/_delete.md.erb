## Delete a sub account

Delete the sub account.

> Definition

```shell
POST <%= config[:api_host] %>ma/account/delete
```

```javascript
Zoom.account.delete();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>ma/account/delete \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d account_id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

var account = {
    account_id: "<%= config[:ex_uuid] %>"
}

Zoom.account.delete(account, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "id": "<%= config[:ex_uuid] %>",
  "deleted_at": "2012-11-25T12:00:00Z"
}
```

Arguments | &nbsp;
--- | ---
account_id **required** | The sub account ID.