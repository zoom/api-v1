## Edit A Group

Edit a group on Zoom, return the new group info.

> Definition

```text
POST <%= config[:api_host] %>group/edit
```

> Example Request

```shell
$ curl <%= config[:api_host] %>group/edit \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```


> Example Response

```json
{
  "group_id": "<%= config[:ex_uuid] %>",
  "account_id": "<%= config[:ex_uuid] %>",
  "name": "New Group Test",
  "total_members": 0
}
```

Arguments | &nbsp;
--- | ---
id **required** | Group ID.
name *optional* | Group name, must be unique in one account.