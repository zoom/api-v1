## List Group Members

Lists the members of a group on Zoom.

> Definition

```shell
POST <%= config[:api_host] %>group/member/list
```

```javascript
Zoom.group.memberList();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>group/member/list \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %> \
    -d id=<%= config[:ex_uuid] %>
```

```javascript
<%= config[:ex_node_init] %>

var group = {
    id: "<%= config[:ex_uuid] %>"
}

Zoom.group.memberList(group, <%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 1,
  "members": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "email": "<%= config[:ex_email] %>",
      "first_name": "<%= config[:ex_first_name] %>",
      "last_name": "<%= config[:ex_last_name] %>",
      "pic_url": "https://ssotest.zoom.us/p/6EoubcvJR7eF2grNXJkrHA/f2003a9c-3095-4865-8536-03cc6f211e86-9569"
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
id **required** | Group ID.
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*