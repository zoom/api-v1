## List H.323/SIP Devices

List H.323/SIP Devices on your Zoom account.

> Definition

```shell
POST <%= config[:api_host] %>h323/device/list
```

```javascript
Zoom.device.list();
```

> Example Request

```shell
$ curl <%= config[:api_host] %>h323/device/list \
    -X POST \
    -d api_key=<%= config[:ex_api_key] %> \
    -d api_secret=<%= config[:ex_api_secret] %>
```

```javascript
<%= config[:ex_node_init] %>

Zoom.device.list(<%= config[:ex_node_res] %>);
```

> Example Response

```json
{
  "page_count": 1,
  "page_number": 1,
  "page_size": 30,
  "total_records": 1,
  "devices": [
    {
      "id": "<%= config[:ex_uuid] %>",
      "name": "New Device",
      "ip": "192.168.1.10",
      "protocol_type": 1,
      "encryption_type": 2
    }
  ]
}
```

Arguments | &nbsp;
--- | ---
page_size *optional* | The amount of records returns within a single API call. Defaults to 30. *Default: 30*
page_number *optional* | Current page number of returned records. Default to 1. *Default: 1*